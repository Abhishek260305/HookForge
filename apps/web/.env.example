# Auth.js — required for sessions; GitHub optional for local scaffold
AUTH_SECRET=replace-with-openssl-rand-hex-32

# GitHub OAuth (optional). When unset, dashboard auth is bypassed locally.
# AUTH_GITHUB_ID=
# AUTH_GITHUB_SECRET=

# GraphQL gateway
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:8080/graphql
