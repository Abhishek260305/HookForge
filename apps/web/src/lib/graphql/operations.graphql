query Health {
  health
}

query Projects {
  projects {
    id
    name
    slug
    environment
    createdAt
    updatedAt
  }
}

mutation CreateProject($input: CreateProjectInput!) {
  createProject(input: $input) {
    id
    name
    slug
    environment
    createdAt
    updatedAt
  }
}
